apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chatflow-member.fullname" . }}-test-connection"
  labels:
    {{- include "chatflow-member.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chatflow-member.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
