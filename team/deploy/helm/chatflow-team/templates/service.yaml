apiVersion: v1
kind: Service
metadata:
  name: {{ include "chatflow-team.fullname" . }}
  labels:
    {{- include "chatflow-team.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chatflow-team.selectorLabels" . | nindent 4 }}
