apiVersion: v1
kind: Service
metadata:
  name: {{ include "team.fullname" . }}-active
  labels:
    {{- include "team.labels" . | nindent 4 }}
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "team.selectorLabels" . | nindent 4 }}